# Artifacts

Checkpoints, activation tensors, reconstructed model states, caches, and temporary run
artifacts live here locally and are ignored by Git. Curated lightweight evidence belongs in
`results/`; large release artifacts should use an external artifact store or GitHub release.
