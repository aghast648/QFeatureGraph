# Results

Commit only curated, lightweight and reproducible outputs: aggregate metrics, graph tables,
selected subsets, configuration snapshots, manifests, and final figures. Raw runs are stored
under ignored `results/runs/`. Never hand-edit generated result values.
