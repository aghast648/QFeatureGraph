# Source Layout

Production scientific logic will live in the `qfeaturegraph` package. The planned package
directories are tracked now through small policy READMEs because Git does not track empty
directories. Replace each placeholder with implementation and focused module documentation
as coding begins; do not put core logic in notebooks or ad hoc scripts.
