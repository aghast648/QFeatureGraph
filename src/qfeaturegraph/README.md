# qfeaturegraph Package

Planned top-level modules are `cli.py`, `config.py`, and `reproducibility.py`. Subpackages
separate data, models, graph construction, optimization, pruning, and evaluation. No Python
module is committed at the scaffold stage so the first executable code can arrive with its
tests.
