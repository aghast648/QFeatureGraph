# Configurations

Configuration files are immutable experiment contracts. `poc_v1.yaml` defines the
canonical proof-of-concept and `smoke_test.yaml` defines a fast end-to-end check.

Once a configuration has produced reported results, do not overwrite it. Create a
new versioned file and record the reason in `docs/decisions.md`.
